# Python dependencies for Streamlit Community Cloud.
# Most versions are intentionally unpinned so the cloud builder can resolve
# compatible wheels for the selected Python runtime. Kaleido is pinned because
# newer releases require a separate Chrome install for Plotly image export.
streamlit
pandas
plotly
kaleido==0.2.1
matplotlib
reportlab
openpyxl
requests
gspread
google-auth
