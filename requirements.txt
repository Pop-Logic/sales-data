# Python dependencies for Streamlit Community Cloud.
# Versions are intentionally unpinned so the cloud builder can resolve
# compatible wheels for the selected Python runtime.
streamlit
pandas
plotly
kaleido
matplotlib
reportlab
openpyxl
requests
gspread
google-auth
